# Raw .wav drops are transient build input. Upload them here via GitHub
# (they will be committed to the branch, then removed once converted to the
# final MP3s under public/sounds/). This folder intentionally does NOT ignore
# .wav files, so GitHub's "Upload files" can commit them for the converter.
