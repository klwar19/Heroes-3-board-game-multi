# Wuxia mod plan — MOVED (merged into the unified Anime mod plan)

> **This plan has been merged into ONE mod:** see
> **`docs/anime-mod-plan.md`** — "Anime mod — one mod, two theme packages:
> **Ninefold Realms** (xianxia) × **Otherworld Gate** (isekai)".
>
> Per the user's direction the wuxia/xianxia design and the anime/isekai
> design are now a single mod behind one crest: either theme package can be
> selected on its own, any module mix is legal, and the two work together as
> a whole (shared options block, story system, campaign hub, destiny/karma
> substrate, quest vocabulary, skin architecture, and a cross-package
> synergy layer).
>
> Everything this document used to contain lives on there, updated:
> - The four xianxia towns, Secret Realms, neutrals, Elixir Pills,
>   Cultivation/Tribulation, Destiny & Karma → `docs/anime-mod-plan.md` §5.
> - Commanders (all towns) → §7. Quest Guards → §9. Spell-Book compat → §10.
> - The visual-novel story system → §11; the Chen Fan campaign
>   ("The Jianghu Chronicle") → §12.1; the System/Golden Fingers → §13.
> - The previously planned separate `WuxiaModOptions` block is superseded by
>   the unified `AnimeModOptions` (toggle mapping: `wuxia.towns` →
>   `anime.xianxiaTowns`, `wuxia.secretRealms` → `anime.secretRealms`,
>   `wuxia.neutrals` → `anime.xianxiaNeutrals`, `wuxia.elixirPills` →
>   `anime.elixirPills`, `wuxia.cultivation` → `anime.cultivation`,
>   `wuxia.destiny` → `anime.destiny`; `wuxia.questGuards` is designer
>   content with no toggle).
>
> STATUS of the merged plan: DESIGN ONLY — nothing implemented. Do not build
> from this stub; build from `docs/anime-mod-plan.md`.
