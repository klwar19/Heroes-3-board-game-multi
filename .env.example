# Supabase and production multiplayer variables will be added in later phases.
NEXT_PUBLIC_APP_NAME="Heroes 3 Board Game Multi"

# PartyKit / Cloudflare Durable Objects multiplayer backend (optional).
# Deploy with `npx partykit deploy` and put the printed host here; leave unset
# to use the built-in Next.js room store + SSE stream.
# NEXT_PUBLIC_PARTYKIT_HOST="heroes3bg-rooms.<your-user>.partykit.dev"
